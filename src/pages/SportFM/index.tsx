import * as React from "react";
import { Layout } from "@/layout";

interface Props {}

/**
 * 云音乐官方歌单
 * TODO: 做成可替换的
 */
const SportFM: React.FunctionComponent<Props> = (props) => {
	return (
		<Layout>
			<div>测试接口</div>
		</Layout>
	);
};

export default SportFM;
