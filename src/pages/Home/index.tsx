import { useContext } from 'react';
import { Layout } from '@/layout';
import { Redirect } from 'react-router-dom';

/**
 * 主页
 */
const Home = () => {
  return <Layout>测试</Layout>;
};

export default Home;
